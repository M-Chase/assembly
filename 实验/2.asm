;编写一个累计减法，被减数是10011000B，减数是 01000000B，连续减5次，观察FLAGS的变化
;00100000->00100000->00100010->00110011->00110000->00110000
DATA SEGMENT
    
DATA ENDS

STACKS SEGMENT
    
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES, DS:DATA, SS:STACKS
START:
    mov al,10011000b
    mov ah,00000000b
    mov bl,01000000b
    mov bh,00000000b

    mov cx,5
s:  sub ax,bx
    loop s
    
    MOV AH, 4CH
    INT 21H
CODES ENDS
END START